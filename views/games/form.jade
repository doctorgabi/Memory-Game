form#newGame(action='/games/start', method='post')
  fieldset
    legend New Game
    .row
      mixin field(8, 'player', 'Player name?', null, 'Sara', true)
      mixin field(4, 'numMatches', 'How many pairs?', null, '5', false)
      .small-4.columns: button.radius.success.small.expand(type='submit') Start